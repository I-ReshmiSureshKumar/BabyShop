<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BabyBloom - Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --pink-100: #ffe6f2;
            --pink-300: #ffb3d9;
            --accent: #d24d8f;
            --muted: #7a5b8b;
            --card-bg: #ffffff;
        }

        body {
            background: linear-gradient(to bottom, #fff8fb, #f7e9ff);
            font-family: 'Poppins', sans-serif;
            margin: 0;
        }

        /* NAVBAR */
        .site-navbar {
            background: #f9d4ea;
            border-bottom: 3px solid var(--pink-300);
            z-index: 1000;
            position: sticky;
            top: 0;
            width: 100%;
        }

            .site-navbar .container {
                display: flex;
                align-items: center;
                gap: 16px;
                padding: 10px 20px;
            }

        .brand {
            color: var(--accent);
            font-weight: 700;
            font-size: 1.45rem;
            white-space: nowrap;
        }

        /* Left nav, center menu, right utilities — always visible in one row on >=576px */
        .nav-left {
            flex: 0 0 auto
        }

        .nav-center {
            flex: 1 1 auto;
            display: flex;
            justify-content: flex-start; /* Moves menu to left */
            margin-left: 40px; /* Optional small spacing */
        }


        .nav-right {
            flex: 0 0 auto;
            display: flex;
            align-items: center;
            gap: 12px
        }

        .nav-center .nav-link {
            color: var(--accent);
            font-weight: 600;
            white-space: nowrap;
            padding: 8px 12px
        }

        .nav-center .dropdown-menu {
            min-width: 200px
        }

        .search-input {
            width: 220px
        }

        .icon-btn {
            font-size: 1.35rem;
            color: var(--accent);
            text-decoration: none
        }

        .account-btn {
            background: #ff80bf;
            color: #fff;
            border-radius: 20px;
            padding: 8px 16px;
            border: none
        }

        /* ensure navbar content doesn't wrap on medium+ screens */
        @@media(min - width:576px) {
            .site-navbar .container

        {
            flex-wrap: nowrap
        }

        .navbar-toggler {
            display: none
        }

        }

        /* mobile: stack menu into collapse */
        @@media(max - width:575.98px) {
            .nav-center

        {
            display: none
        }

        .nav-right .search-input {
            display: none
        }

        .search-input {
            width: 100%
        }

        }

        /* HERO */
        .hero {
            min-height: 72vh;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 30px;
            padding: 40px 6%;
        }

        .hero-text h1 {
            font-size: 2.6rem;
            color: #5a3d69;
            margin: 0
        }

        .hero-text p {
            color: var(--muted);
            margin-top: 12px
        }

        .cta {
            background: #e98bcf;
            border: none;
            padding: 12px 28px;
            border-radius: 26px;
            color: #fff;
            font-weight: 600
        }

        /* categories */
        .categories {
            padding: 40px 6% 80px
        }

        .category-card {
            background: var(--card-bg);
            border-radius: 14px;
            padding: 28px;
            text-align: center;
            box-shadow: 0 6px 18px rgba(0,0,0,0.06);
            transition: transform .25s
        }

            .category-card:hover {
                transform: translateY(-6px)
            }

    </style>
</head>
<body>

    <!-- TOP NAVBAR (Brand + Account + Cart) -->
    <header class="site-navbar">
        <div class="container d-flex align-items-center py-2">
            <a class="brand" href="#" style="text-decoration:none">🌸 BabyBloom</a>

            <!-- CENTER MENU (Option A) -->
            <div class="nav-center d-none d-sm-flex">
                <a class="nav-link" asp-controller="Home" asp-action="Index">Home</a>
                <div class="dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">Shop</a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">New Arrivals</a></li>
                        <li><a class="dropdown-item" href="#">Baby Clothing</a></li>
                        <li><a class="dropdown-item" href="#">Toys</a></li>
                    </ul>
                </div>
                <div class="dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">Categories</a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">Feeding</a></li>
                        <li><a class="dropdown-item" href="#">Bathing</a></li>
                        <li><a class="dropdown-item" href="#">Accessories</a></li>
                    </ul>
                </div>
                <a class="nav-link" href="#">About</a>
                <a class="nav-link" href="#">Contact</a>
            </div>

            <div class="d-flex align-items-center gap-3">
                <a class="icon-btn" href="#" title="Cart">🛒</a>
                <div class="dropdown">
                    <button class="account-btn" data-bs-toggle="dropdown">🔐 Account</button>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="/Admin/AdminLogin">Sign In</a></li>
                        <li><a class="dropdown-item" href="/Admin/UserRegistration">Sign Up</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </header>



    <main class="container py-4">
        @RenderBody()
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
